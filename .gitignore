# Raw captures and processing data (hundreds of GB, not source)
/Capture/
/Sun/
/umbra/
/out/
/swap/

# Python bytecode
__pycache__/
*.pyc

# Virtualenv (uv-managed; recreate with `uv sync` in pipeline/)
.venv/
uv.lock
